#
# This source file is part of the Stanford Spezi open-source project
#
# SPDX-FileCopyrightText: 2026 Stanford University and the project authors (see CONTRIBUTORS.md)
#
# SPDX-License-Identifier: MIT
#

# List of logical sub-packages within the monorepo
#
# Each entry is a logical sub-package within the monorepo: a collection of targets that belong
# together to implement a set of APIs. Used to map changed files -> affected sub-package -> tests.
#   platforms = the platforms the sub-package's upstream repo tested on (CI may schedule a subset).
#               "Linux" is recorded where upstream ran `swift test` on Linux; it is not yet scheduled
#               (xcodebuild CI is Apple-only) and SpeziAccount lists iOS deliberately (some tests fail).
#   targets   = its source target directories  (Sources/<name>)
#   tests     = its test target directories     (Tests/<name>; a Tests/<name>/UITests/ subfolder counts too)
#   uiTests   = the platforms its UITests Xcode project runs on (absent = no UI tests).
#   self-hosted-ci = which test kinds run on the self-hosted runner (vs GitHub-hosted): a subset of
#               ["unit", "ui"]. Optional; defaults to ["ui"] — UI tests on the self-hosted runner,
#               unit tests on GitHub-hosted (today's behavior). Add "unit" (e.g. ["unit", "ui"]) to
#               also route a package's unit tests to the self-hosted runner — useful for tests that
#               need more CPU cores (e.g. RuntimeAssertions, whose cooperative-pool-blocking tests
#               starve on the few-vCPU GitHub runner). Linux unit jobs always stay on GitHub-hosted
#               ubuntu regardless (the self-hosted runner is macOS).
# Note: SpeziFoundation's "CZlib" is a directory name (Sources/CZlib); its Swift target is "SpeziCZlib".

[FHIRModelsExtensions]
platforms = ["iOS", "macOS", "macCatalyst", "watchOS", "visionOS", "tvOS"]
targets = ["FHIRModelsExtensions", "FHIRPathParser", "FHIRQuestionnaires"]
tests = ["FHIRModelsExtensionsTests", "FHIRPathParserTests"]

[ResearchKitOnFHIR]
platforms = ["iOS"]
targets = ["ResearchKitOnFHIR"]
tests = ["ResearchKitOnFHIRTests"]
uiTests = ["iOS"]

[Spezi]
platforms = ["iOS", "macOS", "macCatalyst", "watchOS", "visionOS", "tvOS", "Linux"]
targets = ["Spezi", "SpeziTesting", "XCTSpezi"]
tests = ["SpeziTests"]
uiTests = ["iOS", "visionOS"]

[SpeziAccessGuard]
platforms = ["iOS"]
targets = ["SpeziAccessGuard"]
tests = ["SpeziAccessGuardTests"]
uiTests = ["iOS"]

[SpeziAccount]
platforms = ["iOS", "macOS"]
targets = ["SpeziAccount", "SpeziAccountMacros", "SpeziAccountPhoneNumbers", "XCTSpeziAccount"]
tests = ["SpeziAccountTests", "SpeziAccountMacrosTests"]
uiTests = ["iOS", "iPadOS", "visionOS"]

[SpeziBluetooth]
platforms = ["iOS", "macOS", "macCatalyst", "watchOS", "visionOS", "tvOS"]
targets = ["SpeziBluetooth", "SpeziBluetoothServices", "TestPeripheral"]
tests = ["SpeziBluetoothTests", "SpeziBluetoothServicesTests"]
uiTests = ["iOS"]

[SpeziChat]
platforms = ["iOS", "macOS", "visionOS"]
targets = ["SpeziChat"]
tests = ["SpeziChatTests"]
uiTests = ["iOS", "visionOS"]

[SpeziConsent]
platforms = ["iOS", "macOS", "visionOS"]
targets = ["SpeziConsent"]
tests = ["SpeziConsentTests"]
uiTests = ["iOS", "iPadOS", "visionOS"]

[SpeziContact]
platforms = ["iOS"]
targets = ["SpeziContact"]
tests = ["SpeziContactTests"]
uiTests = ["iOS"]

[SpeziDevices]
platforms = ["iOS", "macOS", "macCatalyst", "visionOS"]
targets = ["SpeziDevices", "SpeziDevicesUI", "SpeziOmron"]
tests = ["SpeziDevicesTests", "SpeziOmronTests"]
uiTests = ["iOS"]

[SpeziFHIR]
platforms = ["iOS", "macOS"]
targets = ["SpeziFHIR", "SpeziFHIRMockPatients"]
tests = ["SpeziFHIRTests"]
uiTests = ["iOS"]

[SpeziFileFormats]
platforms = ["iOS", "macOS", "watchOS", "visionOS", "tvOS"]
targets = ["SpeziFileFormats", "EDFFormat"]
tests = ["EDFFormatTests"]

[SpeziFirebase]
platforms = ["iOS"]
targets = ["SpeziFirebase", "SpeziFirebaseAccount", "SpeziFirebaseAccountStorage", "SpeziFirebaseConfiguration", "SpeziFirebaseStorage", "SpeziFirestore"]
tests = ["SpeziFirebaseTests"]
uiTests = ["iOS"]

[SpeziFoundation]
platforms = ["iOS", "macOS", "macCatalyst", "watchOS", "visionOS", "tvOS", "Linux"]
targets = ["SpeziFoundation", "CZlib", "SpeziFoundationObjC", "SpeziLocalization"]
tests = ["SpeziFoundationTests", "SpeziLocalizationTests"]
uiTests = ["iOS"]

[SpeziHealthKit]
platforms = ["iOS", "macOS", "macCatalyst", "watchOS", "visionOS", "Linux"]
targets = ["SpeziHealthKit", "Codegen", "LocalizationsProcessor", "SpeziHealthKitBulkExport", "SpeziHealthKitUI"]
tests = ["SpeziHealthKitTests"]
uiTests = ["iOS"]

# Formerly the standalone HealthKitOnFHIR package; merged into SpeziHealthKit by its PR #86 (see
# Package.swift). Kept as its own scheduling entry rather than folded into [SpeziHealthKit], so its
# UITests project keeps a home and CI stays granular -- the test matrix is directory-based.
[SpeziHealthKitFHIR]
platforms = ["iOS", "macOS", "watchOS"]
targets = ["SpeziHealthKitFHIR", "SpeziHealthKitFHIRMacros", "SpeziHealthKitFHIRMacrosImpl"]
tests = ["SpeziHealthKitFHIRTests", "SpeziHealthKitFHIRMacrosTests"]
uiTests = ["iOS"]

[SpeziLLM]
platforms = ["iOS", "macOS", "visionOS"]
targets = ["SpeziLLM", "GeneratedOpenAIClient", "SpeziLLMAnthropic", "SpeziLLMFog", "SpeziLLMGemini", "SpeziLLMLocal", "SpeziLLMLocalDownload", "SpeziLLMOpenAI", "SpeziLLMOpenAIRealtime"]
tests = ["SpeziLLMTests"]
uiTests = ["iOS", "iPadOS", "visionOS"]

[SpeziLicense]
platforms = ["iOS"]
targets = ["SpeziLicense"]
tests = ["SpeziLicenseTests"]
uiTests = ["iOS", "iPadOS"]

[SpeziLocation]
platforms = ["iOS", "watchOS"]
targets = ["SpeziLocation"]
tests = ["SpeziLocationTests"]
uiTests = ["iOS", "iPadOS"]

[SpeziNetworking]
platforms = ["iOS", "macOS", "watchOS", "visionOS", "tvOS"]
targets = ["SpeziNetworking", "ByteCoding", "ByteCodingTesting", "SpeziNumerics", "XCTByteCoding"]
tests = ["ByteCodingTests", "SpeziNumericsTests"]

[SpeziNotifications]
platforms = ["iOS", "macOS", "watchOS", "visionOS", "tvOS"]
targets = ["SpeziNotifications", "XCTSpeziNotifications", "XCTSpeziNotificationsUI"]
tests = ["SpeziNotificationsTests"]
uiTests = ["iOS", "visionOS"]

[SpeziOnboarding]
platforms = ["iOS", "macOS", "visionOS"]
targets = ["SpeziOnboarding"]
tests = ["SpeziOnboardingTests"]
uiTests = ["iOS", "iPadOS", "visionOS"]

[SpeziQuestionnaire]
platforms = ["iOS", "macOS"]
targets = ["SpeziQuestionnaire", "SpeziQuestionnaireCatalog", "SpeziQuestionnaireFHIR", "SpeziQuestionnaireLegacy", "XCTSpeziQuestionnaire"]
tests = ["SpeziQuestionnaireTests"]
uiTests = ["iOS", "iPadOS"]

[SpeziScheduler]
platforms = ["iOS", "macOS", "watchOS", "visionOS"]
targets = ["SpeziScheduler", "SpeziSchedulerMacros", "SpeziSchedulerUI"]
tests = ["SpeziSchedulerTests", "SpeziSchedulerUITests"]
uiTests = ["iOS"]

[SpeziSensorKit]
platforms = ["iOS"]
targets = ["SpeziSensorKit"]
tests = ["SpeziSensorKitTests"]
uiTests = ["iOS"]

[SpeziSpeech]
platforms = ["iOS", "macOS", "visionOS"]
targets = ["SpeziSpeech", "SpeziSpeechRecognizer", "SpeziSpeechSynthesizer"]
tests = ["SpeziSpeechTests"]
uiTests = ["iOS", "iPadOS", "visionOS"]

[SpeziStorage]
platforms = ["iOS", "macOS", "macCatalyst", "watchOS", "visionOS"]
targets = ["SpeziStorage", "SpeziKeychainStorage", "SpeziLocalStorage"]
tests = ["SpeziStorageTests"]
uiTests = ["iOS", "iPadOS", "visionOS"]

[SpeziStudy]
platforms = ["iOS", "macOS", "macCatalyst", "watchOS", "visionOS", "Linux"]
targets = ["SpeziStudy", "SpeziStudyDefinition"]
tests = ["SpeziStudyTests"]
uiTests = ["iOS"]

[SpeziViews]
platforms = ["iOS", "macOS", "watchOS", "visionOS", "tvOS"]
targets = ["SpeziViews", "SpeziPersonalInfo", "SpeziValidation"]
tests = ["SpeziViewsTests"]
uiTests = ["iOS", "iPadOS", "visionOS"]

[ThreadLocal]
platforms = ["iOS", "macOS", "macCatalyst", "watchOS", "visionOS", "tvOS"]
targets = ["ThreadLocal", "ThreadLocalMacros"]
tests = ["ThreadLocalTests"]

[XCTHealthKit]
platforms = ["iOS"]
targets = ["XCTHealthKit"]
tests = ["XCTHealthKitTests"]
uiTests = ["iOS"]

[RuntimeAssertions]
platforms = ["iOS", "macOS", "macCatalyst", "watchOS", "visionOS", "tvOS"]
targets = ["RuntimeAssertions", "RuntimeAssertionsTesting"]
tests = ["RuntimeAssertionsTests"]
self-hosted-ci = ["ui", "unit"]
# This package's self-hosted jobs run run-package-tests.sh's tomllib path (Python 3.11+); pin them
# to a self-hosted runner carrying the `python3.11+` label (older self-hosted machines have < 3.11).
extra_runner_labels = ["python3.11+"]

[XCTestExtensions]
platforms = ["iOS", "macOS", "watchOS", "visionOS"]
targets = ["XCTestExtensions", "XCTestApp"]
tests = ["XCTestExtensionsTests"]
uiTests = ["iOS", "iPadOS", "visionOS"]
